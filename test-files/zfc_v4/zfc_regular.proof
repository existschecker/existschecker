include "zfc_tcm.proof"

definition predicate hirrefrel(x) as \exists y (irrefrel(y, in) \wedge tcm(x, y))

theorem hirrefrel_is_irrefrel \forall x (hirrefrel(x) \to irrefrel(x, in)) {
  any x {
    assume hirrefrel(x) {
      some y such hirrefrel(x) {
        split irrefrel(y, in) \wedge tcm(x, y)
        apply invoke tcm_implies_subset for x, y
        connect irrefrel(y, in) \wedge subset(x, y)
        apply invoke subset_of_irrefrel_is_irrefrel for in, y, x
      }
    }
  }
}

theorem element_of_hirrefrel_is_hirrefrel \forall x \forall y (hirrefrel(x) \wedge in(y, x) \to hirrefrel(y)) {
  any x, y {
    assume hirrefrel(x) \wedge in(y, x) {
      split hirrefrel(x) \wedge in(y, x)
      some t such hirrefrel(x) {
        split irrefrel(t, in) \wedge tcm(x, t)
        connect tcm(x, t) \wedge in(y, x)
        apply invoke tcm_of_element_is_subset_of_tcm for x, t, y
        some s such \exists s (subset(s, t) \wedge tcm(y, s)) {
          split subset(s, t) \wedge tcm(y, s)
          connect irrefrel(t, in) \wedge subset(s, t)
          apply invoke subset_of_irrefrel_is_irrefrel for in, t, s
          connect irrefrel(s, in) \wedge tcm(y, s)
          lift for s conclude hirrefrel(y)
        }
      }
    }
  }
}

theorem set_of_hirrefrel_is_hirrefrel \forall Y (\forall y (in(y, Y) \to hirrefrel(y)) \wedge \exists Z \exists W \exists V (tcmstep(Y, Z, W, V)) \to hirrefrel(Y)) {
  any Y {
    assume \forall y (in(y, Y) \to hirrefrel(y)) \wedge \exists Z \exists W \exists V (tcmstep(Y, Z, W, V)) {
      split \forall y (in(y, Y) \to hirrefrel(y)) \wedge \exists Z \exists W \exists V (tcmstep(Y, Z, W, V))
      some Z, W, V such \exists Z \exists W \exists V (tcmstep(Y, Z, W, V)) {
        split tcmstep(Y, Z, W, V)
        apply invoke tcm_is_cup_of_itself_and_bigcup_of_tcm_of_element for Y, Z, W, V
        any y {
          assume in(y, Y) {
            apply invoke \forall y (in(y, Y) \to hirrefrel(y)) for y
            apply invoke hirrefrel_is_irrefrel for y
            apply invoke irrefrel_not_contain_itself for y
          }
        }
        fold for irrefrel conclude irrefrel(Y, in)
        any w {
          assume in(w, W) {
            apply invoke rightward bigcup(Z, W) for w
            some z such \exists z (in(w, z) \wedge in(z, Z)) {
              split in(w, z) \wedge in(z, Z)
              apply invoke rightward replacement(Y, tcm, Z) for z
              some y such \exists y (in(y, Y) \wedge tcm(y, z)) {
                split in(y, Y) \wedge tcm(y, z)
                apply invoke \forall y (in(y, Y) \to hirrefrel(y)) for y
                some z' such hirrefrel(y) {
                  split irrefrel(z', in) \wedge tcm(y, z')
                  connect tcm(y, z) \wedge tcm(y, z')
                  apply invoke tcm_is_unique for y, z, z'
                  substitute irrefrel(z', in) for z': z
                  apply invoke irrefrel(z, in) for w
                }
              }
            }
          }
        }
        fold for irrefrel conclude irrefrel(W, in)
        connect irrefrel(Y, in) \wedge irrefrel(W, in) \wedge cup(Y, W, V)
        apply invoke cup_of_irrefrel_is_irrefrel for in, Y, W, V
        connect irrefrel(V, in) \wedge tcm(Y, V)
        lift for V conclude hirrefrel(Y)
      }
    }
  }
}

definition predicate hwellfounded(x) as \exists y (wellfounded(y, in) \wedge tcm(x, y))

theorem hwellfounded_is_wellfounded \forall x (hwellfounded(x) \to wellfounded(x, in)) {
  any x {
    assume hwellfounded(x) {
      some y such hwellfounded(x) {
        split wellfounded(y, in) \wedge tcm(x, y)
        apply invoke tcm_implies_subset for x, y
        connect wellfounded(y, in) \wedge subset(x, y)
        apply invoke subset_of_wellfounded_is_wellfounded for in, y, x
      }
    }
  }
}

theorem element_of_hwellfounded_is_hwellfounded \forall x \forall y (hwellfounded(x) \wedge in(y, x) \to hwellfounded(y)) {
  any x, y {
    assume hwellfounded(x) \wedge in(y, x) {
      split hwellfounded(x) \wedge in(y, x)
      some t such hwellfounded(x) {
        split wellfounded(t, in) \wedge tcm(x, t)
        connect tcm(x, t) \wedge in(y, x)
        apply invoke tcm_of_element_is_subset_of_tcm for x, t, y
        some s such \exists s (subset(s, t) \wedge tcm(y, s)) {
          split subset(s, t) \wedge tcm(y, s)
          connect wellfounded(t, in) \wedge subset(s, t)
          apply invoke subset_of_wellfounded_is_wellfounded for in, t, s
          connect wellfounded(s, in) \wedge tcm(y, s)
          lift for s conclude hwellfounded(y)
        }
      }
    }
  }
}

theorem set_of_hwellfounded_is_hwellfounded \forall Y (\forall y (in(y, Y) \to hwellfounded(y)) \wedge \exists Z \exists W \exists V (tcmstep(Y, Z, W, V)) \to hwellfounded(Y)) {
  any Y {
    assume \forall y (in(y, Y) \to hwellfounded(y)) \wedge \exists Z \exists W \exists V (tcmstep(Y, Z, W, V)) {
      split \forall y (in(y, Y) \to hwellfounded(y)) \wedge \exists Z \exists W \exists V (tcmstep(Y, Z, W, V))
      some Z, W, V such \exists Z \exists W \exists V (tcmstep(Y, Z, W, V)) {
        split tcmstep(Y, Z, W, V)
        apply invoke tcm_is_cup_of_itself_and_bigcup_of_tcm_of_element for Y, Z, W, V
        any A {
          assume subset(A, V) \wedge neq(A, emptyset) {
            split subset(A, V) \wedge neq(A, emptyset)
            apply invoke not_emptyset_has_a_member for A
            some v such \exists v (in(v, A)) {
              apply invoke subset(A, V) for v
              apply invoke rightward cup(Y, W, V) for v
              divide in(v, Y) \vee in(v, W)
                case in(v, Y) {
                  apply invoke \forall y (in(y, Y) \to hwellfounded(y)) for v
                }
                case in(v, W) {
                  apply invoke rightward bigcup(Z, W) for v
                  some z such \exists z (in(v, z) \wedge in(z, Z)) {
                    split in(v, z) \wedge in(z, Z)
                    apply invoke rightward replacement(Y, tcm, Z) for z
                    some y such \exists y (in(y, Y) \wedge tcm(y, z)) {
                      split in(y, Y) \wedge tcm(y, z)
                      apply invoke \forall y (in(y, Y) \to hwellfounded(y)) for y
                      some z' such hwellfounded(y) {
                        split wellfounded(z', in) \wedge tcm(y, z')
                        apply invoke tcm_is_tcm_of_itself for y, z'
                        connect wellfounded(z', in) \wedge tcm(z', z')
                        lift for z' conclude hwellfounded(z')
                        connect tcm(y, z) \wedge tcm(y, z')
                        apply invoke tcm_is_unique for y, z, z'
                        substitute hwellfounded(z') for z': z
                        connect hwellfounded(z) \wedge in(v, z)
                        apply invoke element_of_hwellfounded_is_hwellfounded for z, v
                      }
                    }
                  }
                }
              some T such hwellfounded(v) {
                split wellfounded(T, in) \wedge tcm(v, T)
                apply invoke tcm_implies_subset for v, T
                expand tcm(v, T) for tcm
                split tcset(v, in, T)
                apply law_of_excluded_middle for \lambda^P. equal(cap(A, T), emptyset)
                divide equal(cap(A, T), emptyset) \vee neq(cap(A, T), emptyset)
                  case equal(cap(A, T), emptyset) {
                    any x {
                      assume in(x, A) \wedge neq(x, v) {
                        split in(x, A) \wedge neq(x, v)
                        deny in(x, v) {
                          apply invoke subset(v, T) for x
                          connect in(x, A) \wedge in(x, T)
                          apply invoke leftward cap_characterization for A, T, x
                          substitute in(x, cap(A, T)) for cap(A, T): emptyset
                          apply emptyset_existence for x
                          contradict in(x, emptyset)
                        }
                      }
                    }
                    fold for minimal_core conclude minimal_core(in, A, v)
                    connect minimal(in, A, v)
                    lift for v conclude \exists a (minimal(in, A, a))
                  }
                  case neq(cap(A, T), emptyset) {
                    apply cap_is_subset_right for A, T
                    connect subset(cap(A, T), T) \wedge neq(cap(A, T), emptyset)
                    apply invoke wellfounded(T, in) for cap(A, T)
                    some a such \exists a (minimal(in, cap(A, T), a)) {
                      split minimal(in, cap(A, T), a)
                      apply invoke rightward cap_characterization for A, T, a
                      split in(a, A) \wedge in(a, T)
                      any x {
                        assume in(x, A) \wedge neq(x, a) {
                          split in(x, A) \wedge neq(x, a)
                          deny in(x, a) {
                            connect in(x, a) \wedge in(a, T)
                            apply invoke leftward trans_iff_closed_in for T
                            apply invoke trans(T) for a, x
                            connect in(x, A) \wedge in(x, T)
                            apply invoke leftward cap_characterization for A, T, x
                            connect in(x, cap(A, T)) \wedge neq(x, a)
                            apply invoke minimal_core(in, cap(A, T), a) for x
                            contradict in(x, a)
                          }
                        }
                      }
                      fold for minimal_core conclude minimal_core(in, A, a)
                      connect minimal(in, A, a)
                      lift for a conclude \exists a (minimal(in, A, a))
                    }
                  }
              }
            }
          }
        }
        fold for wellfounded conclude wellfounded(V, in)
        connect wellfounded(V, in) \wedge tcm(Y, V)
        lift for V conclude hwellfounded(Y)
      }
    }
  }
}

definition predicate regular(x) as hirrefrel(x) \wedge hwellfounded(x)

theorem regular_is_irrefrel_wellfounded \forall x (regular(x) \to irrefrel(x, in) \wedge wellfounded(x, in)) {
  any x {
    assume regular(x) {
      split regular(x)
      apply invoke hirrefrel_is_irrefrel for x
      apply invoke hwellfounded_is_wellfounded for x
      connect irrefrel(x, in) \wedge wellfounded(x, in)
    }
  }
}

theorem element_of_regular_is_regular \forall x \forall y (regular(x) \wedge in(y, x) \to regular(y)) {
  any x, y {
    assume regular(x) \wedge in(y, x) {
      split regular(x) \wedge in(y, x)
      split regular(x)
      connect hirrefrel(x) \wedge in(y, x)
      apply invoke element_of_hirrefrel_is_hirrefrel for x, y
      connect hwellfounded(x) \wedge in(y, x)
      apply invoke element_of_hwellfounded_is_hwellfounded for x, y
      connect regular(y)
    }
  }
}

theorem set_of_regular_is_regular \forall Y (\forall y (in(y, Y) \to regular(y)) \wedge \exists Z \exists W \exists V (tcmstep(Y, Z, W, V)) \to regular(Y)) {
  any Y {
    assume \forall y (in(y, Y) \to regular(y)) \wedge \exists Z \exists W \exists V (tcmstep(Y, Z, W, V)) {
      split \forall y (in(y, Y) \to regular(y)) \wedge \exists Z \exists W \exists V (tcmstep(Y, Z, W, V))
      any y {
        assume in(y, Y) {
          apply invoke \forall y (in(y, Y) \to regular(y)) for y
          split 1 regular(y)
        }
      }
      connect \forall y (in(y, Y) \to hirrefrel(y)) \wedge \exists Z \exists W \exists V (tcmstep(Y, Z, W, V))
      apply invoke set_of_hirrefrel_is_hirrefrel for Y
      any y {
        assume in(y, Y) {
          apply invoke \forall y (in(y, Y) \to regular(y)) for y
          split 2 regular(y)
        }
      }
      connect \forall y (in(y, Y) \to hwellfounded(y)) \wedge \exists Z \exists W \exists V (tcmstep(Y, Z, W, V))
      apply invoke set_of_hwellfounded_is_hwellfounded for Y
      connect regular(Y)
    }
  }
}

theorem regular_satisfies_axiom_of_extensionality \forall x \forall y (regular(x) \wedge regular(y) \to (\forall z (regular(z) \to (in(z, x) \leftrightarrow in(z, y))) \to equal(x, y))) {
  any x, y {
    assume regular(x) \wedge regular(y) {
      split regular(x) \wedge regular(y)
      assume \forall z (regular(z) \to (in(z, x) \leftrightarrow in(z, y))) {
        any z {
          assume in(z, x) {
            connect regular(x) \wedge in(z, x)
            apply invoke element_of_regular_is_regular for x, z
            apply invoke \forall z (regular(z) \to (in(z, x) \leftrightarrow in(z, y))) for z
            invoke rightward in(z, x) \leftrightarrow in(z, y)
          }
          assume in(z, y) {
            connect regular(y) \wedge in(z, y)
            apply invoke element_of_regular_is_regular for y, z
            apply invoke \forall z (regular(z) \to (in(z, x) \leftrightarrow in(z, y))) for z
            invoke leftward in(z, x) \leftrightarrow in(z, y)
          }
          connect in(z, x) \leftrightarrow in(z, y)
        }
        apply invoke axiom_of_extensionality for x, y
      }
    }
  }
}

theorem emptyset_is_regular regular(emptyset) {
  assert emptyset_is_tcm_of_emptyset
  assert emptyset_is_irrefrel
  connect irrefrel(emptyset, in) \wedge tcm(emptyset, emptyset)
  lift for emptyset conclude hirrefrel(emptyset)
  assert emptyset_is_wellfounded
  connect wellfounded(emptyset, in) \wedge tcm(emptyset, emptyset)
  lift for emptyset conclude hwellfounded(emptyset)
  connect regular(emptyset)
}

theorem regular_satisfies_axiom_of_emptyset \exists x (regular(x) \wedge \forall y (regular(y) \to notin(y, x))) {
  assert emptyset_is_regular
  any y {
    assume regular(y) {
      apply emptyset_existence for y
    }
  }
  connect regular(emptyset) \wedge \forall y (regular(y) \to notin(y, emptyset))
  lift for emptyset conclude \exists x (regular(x) \wedge \forall y (regular(y) \to notin(y, x)))
}

theorem regular_iff_existence_of_irrefrel_wellfounded_tcm \forall x (regular(x) \leftrightarrow \exists y (irrefrel(y, in) \wedge wellfounded(y, in) \wedge tcm(x, y))) {
  any x {
    assume regular(x) {
      split regular(x)
      some y such hirrefrel(x) {
        split irrefrel(y, in) \wedge tcm(x, y)
        some y' such hwellfounded(x) {
          split wellfounded(y', in) \wedge tcm(x, y')
          connect tcm(x, y) \wedge tcm(x, y')
          apply invoke tcm_is_unique for x, y, y'
          substitute wellfounded(y', in) for y': y
          connect irrefrel(y, in) \wedge wellfounded(y, in) \wedge tcm(x, y)
          lift for y conclude \exists y (irrefrel(y, in) \wedge wellfounded(y, in) \wedge tcm(x, y))
        }
      }
    }
    assume \exists y (irrefrel(y, in) \wedge wellfounded(y, in) \wedge tcm(x, y)) {
      some y such \exists y (irrefrel(y, in) \wedge wellfounded(y, in) \wedge tcm(x, y)) {
        split irrefrel(y, in) \wedge wellfounded(y, in) \wedge tcm(x, y)
        connect irrefrel(y, in) \wedge tcm(x, y)
        lift for y conclude hirrefrel(x)
        connect wellfounded(y, in) \wedge tcm(x, y)
        lift for y conclude hwellfounded(x)
        connect regular(x)
      }
    }
    connect regular(x) \leftrightarrow \exists y (irrefrel(y, in) \wedge wellfounded(y, in) \wedge tcm(x, y))
  }
}

theorem regular_satisfies_axiom_of_separation \forall^P \phi[1] \forall x (regular(x) \to \exists y (regular(y) \wedge \forall z (regular(z) \to (in(z, y) \leftrightarrow in(z, x) \wedge \phi(z))))) {
  any predicate \phi[1], x {
    assume regular(x) {
      apply invoke rightward regular_iff_existence_of_irrefrel_wellfounded_tcm for x
      some p such \exists p (irrefrel(p, in) \wedge wellfounded(p, in) \wedge tcm(x, p)) {
        split irrefrel(p, in) \wedge wellfounded(p, in) \wedge tcm(x, p)
        expand tcm(x, p) for tcm
        split tcset(x, in, p)
        apply axiom_of_separation for x, \phi
        some y such \exists y \forall z (in(z, y) \leftrightarrow in(z, x) \wedge \phi(z)) {
          apply axiom_of_separation for p, \lambda^P z. \forall s (closed(s, in) \wedge subset(y, s) \to in(z, s))
          some q such \exists q \forall z (in(z, q) \leftrightarrow in(z, p) \wedge \forall s (closed(s, in) \wedge subset(y, s) \to in(z, s))) {
            any u, v {
              assume in(u, q) \wedge in(v, u) {
                split in(u, q) \wedge in(v, u)
                apply invoke rightward \forall z (in(z, q) \leftrightarrow in(z, p) \wedge \forall s (closed(s, in) \wedge subset(y, s) \to in(z, s))) for u
                split in(u, p) \wedge \forall s (closed(s, in) \wedge subset(y, s) \to in(u, s))
                connect in(u, p) \wedge in(v, u)
                apply invoke closed(p, in) for u, v
                any s {
                  assume closed(s, in) \wedge subset(y, s) {
                    split closed(s, in) \wedge subset(y, s)
                    apply invoke \forall s (closed(s, in) \wedge subset(y, s) \to in(u, s)) for s
                    connect in(u, s) \wedge in(v, u)
                    apply invoke closed(s, in) for u, v
                  }
                }
                connect in(v, p) \wedge \forall s (closed(s, in) \wedge subset(y, s) \to in(v, s))
                apply invoke leftward \forall z (in(z, q) \leftrightarrow in(z, p) \wedge \forall s (closed(s, in) \wedge subset(y, s) \to in(z, s))) for v
              }
            }
            fold for closed conclude closed(q, in)
            any w {
              assume in(w, y) {
                apply invoke rightward \forall z (in(z, y) \leftrightarrow in(z, x) \wedge \phi(z)) for w
                split in(w, x) \wedge \phi(w)
                apply invoke \forall w (in(w, x) \to in(w, p)) for w
                any s {
                  assume closed(s, in) \wedge subset(y, s) {
                    split 2 closed(s, in) \wedge subset(y, s)
                    apply invoke subset(y, s) for w
                  }
                }
                connect in(w, p) \wedge \forall s (closed(s, in) \wedge subset(y, s) \to in(w, s))
                apply invoke leftward \forall z (in(z, q) \leftrightarrow in(z, p) \wedge \forall s (closed(s, in) \wedge subset(y, s) \to in(z, s))) for w
              }
            }
            any s {
              assume closed(s, in) \wedge \forall z (in(z, y) \to in(z, s)) {
                split closed(s, in) \wedge \forall z (in(z, y) \to in(z, s))
                fold for subset conclude subset(y, s)
                connect closed(s, in) \wedge subset(y, s)
                any z {
                  assume in(z, q) {
                    apply invoke rightward \forall z (in(z, q) \leftrightarrow in(z, p) \wedge \forall s (closed(s, in) \wedge subset(y, s) \to in(z, s))) for z
                    split in(z, p) \wedge \forall s (closed(s, in) \wedge subset(y, s) \to in(z, s))
                    apply invoke \forall s (closed(s, in) \wedge subset(y, s) \to in(z, s)) for s
                  }
                }
                fold for subset conclude subset(q, s)
              }
            }
            connect tcset(y, in, q)
            fold for tcm conclude tcm(y, q)
            any z {
              assume in(z, q) {
                apply invoke rightward \forall z (in(z, q) \leftrightarrow in(z, p) \wedge \forall s (closed(s, in) \wedge subset(y, s) \to in(z, s))) for z
                split 1 in(z, p) \wedge \forall s (closed(s, in) \wedge subset(y, s) \to in(z, s))
              }
            }
            fold for subset conclude subset(q, p)
            connect irrefrel(p, in) \wedge subset(q, p)
            apply invoke subset_of_irrefrel_is_irrefrel for in, p, q
            connect wellfounded(p, in) \wedge subset(q, p)
            apply invoke subset_of_wellfounded_is_wellfounded for in, p, q
            connect irrefrel(q, in) \wedge wellfounded(q, in) \wedge tcm(y, q)
            lift for q conclude \exists q (irrefrel(q, in) \wedge wellfounded(q, in) \wedge tcm(y, q))
          }
          apply invoke leftward regular_iff_existence_of_irrefrel_wellfounded_tcm for y
          any z {
            assume regular(z) {
              apply \forall z (in(z, y) \leftrightarrow in(z, x) \wedge \phi(z)) for z
            }
          }
          connect regular(y) \wedge \forall z (regular(z) \to (in(z, y) \leftrightarrow in(z, x) \wedge \phi(z)))
          lift for y conclude \exists y (regular(y) \wedge \forall z (regular(z) \to (in(z, y) \leftrightarrow in(z, x) \wedge \phi(z))))
        }
      }
    }
  }
}

theorem regular_satisfies_axiom_of_foundation \forall x (regular(x) \to (neq(x, emptyset) \to \exists y (regular(y) \wedge in(y, x) \wedge \forall z (regular(z) \to (in(z, y) \to notin(z, x)))))) {
  any A {
    assume regular(A) {
      assume neq(A, emptyset) {
        split regular(A)
        apply invoke hirrefrel_is_irrefrel for A
        apply invoke hwellfounded_is_wellfounded for A
        apply subset_reflexivity for A
        connect subset(A, A) \wedge neq(A, emptyset)
        apply invoke wellfounded(A, in) for A
        some y such \exists y (minimal(in, A, y)) {
          split minimal(in, A, y)
          connect regular(A) \wedge in(y, A)
          apply invoke element_of_regular_is_regular for A, y
          any z {
            assume regular(z) {
              assume in(z, y) {
                deny in(z, A) {
                  apply law_of_excluded_middle for \lambda^P. equal(z, y)
                  divide equal(z, y) \vee neq(z, y)
                    case equal(z, y) {
                      substitute in(z, y) for z: y
                      apply invoke irrefrel(A, in) for y
                      contradict in(y, y)
                    }
                    case neq(z, y) {
                      connect in(z, A) \wedge neq(z, y)
                      apply invoke minimal_core(in, A, y) for z
                      contradict in(z, y)
                    }
                }
              }
            }
          }
          connect regular(y) \wedge in(y, A) \wedge \forall z (regular(z) \to (in(z, y) \to notin(z, A)))
          lift for y conclude \exists y (regular(y) \wedge in(y, A) \wedge \forall z (regular(z) \to (in(z, y) \to notin(z, A))))
        }
      }
    }
  }
}

theorem regular_implies_tcm_is_regular \forall x \forall t (regular(x) \wedge tcm(x, t) \to regular(t)) {
  any x, t {
    assume regular(x) \wedge tcm(x, t) {
      split regular(x) \wedge tcm(x, t)
      apply invoke rightward regular_iff_existence_of_irrefrel_wellfounded_tcm for x
      some t' such \exists t' (irrefrel(t', in) \wedge wellfounded(t', in) \wedge tcm(x, t')) {
        split irrefrel(t', in) \wedge wellfounded(t', in) \wedge tcm(x, t')
        connect tcm(x, t) \wedge tcm(x, t')
        apply invoke tcm_is_unique for x, t, t'
        substitute irrefrel(t', in) for t': t
        substitute wellfounded(t', in) for t': t
        expand tcm(x, t) for tcm
        split tcset(x, in, t)
        apply invoke rightward closed_in_iff_tcset_of_itself for t
        fold for tcm conclude tcm(t, t)
        connect irrefrel(t, in) \wedge wellfounded(t, in) \wedge tcm(t, t)
        lift for t conclude \exists t' (irrefrel(t', in) \wedge wellfounded(t', in) \wedge tcm(t, t'))
      }
      apply invoke leftward regular_iff_existence_of_irrefrel_wellfounded_tcm for t
    }
  }
}

theorem regular_is_pirrefrel pirrefrel(regular, in) {
  any x {
    assume regular(x) {
      apply invoke regular_is_irrefrel_wellfounded for x
      split 1 irrefrel(x, in) \wedge wellfounded(x, in)
      apply invoke irrefrel_not_contain_itself for x
    }
  }
  fold for pirrefrel conclude pirrefrel(regular, in)
}

theorem regular_is_pwellfounded pwellfounded(regular, in) {
  any A {
    assume \forall x (in(x, A) \to regular(x)) \wedge neq(A, emptyset) {
      split \forall x (in(x, A) \to regular(x)) \wedge neq(A, emptyset)
      apply invoke not_emptyset_has_a_member for A
      some x such \exists x (in(x, A)) {
        apply invoke \forall x (in(x, A) \to regular(x)) for x
        apply invoke rightward regular_iff_existence_of_irrefrel_wellfounded_tcm for x
        some t such \exists t (irrefrel(t, in) \wedge wellfounded(t, in) \wedge tcm(x, t)) {
          split irrefrel(t, in) \wedge wellfounded(t, in) \wedge tcm(x, t)
          expand tcm(x, t) for tcm
          split 1 tcset(x, in, t)
          apply invoke leftward trans_iff_closed_in for t
          apply invoke tcm_implies_subset for x, t
          connect subset(x, t) \wedge trans(t)
          apply invoke subset_trans_implies_trans_adj for x, t
          connect trans(t) \wedge wellfounded(t, in)
          apply invoke adj_of_trans_wellfounded_is_wellfounded for t, x
          apply cap_is_subset_right for A, adj(t, x)
          apply adj_cor3 for t, x
          connect in(x, A) \wedge in(x, adj(t, x))
          apply invoke leftward cap_characterization for A, adj(t, x), x
          lift for x conclude \exists x (in(x, cap(A, adj(t, x))))
          apply invoke has_a_member_not_emptyset for cap(A, adj(t, x))
          connect subset(cap(A, adj(t, x)), adj(t, x)) \wedge neq(cap(A, adj(t, x)), emptyset)
          apply invoke wellfounded(adj(t, x), in) for cap(A, adj(t, x))
          some y such \exists y (minimal(in, cap(A, adj(t, x)), y)) {
            split minimal(in, cap(A, adj(t, x)), y)
            apply invoke rightward cap_characterization for A, adj(t, x), y
            split in(y, A) \wedge in(y, adj(t, x))
            any z {
              assume in(z, A) \wedge neq(z, y) {
                split in(z, A) \wedge neq(z, y)
                apply law_of_excluded_middle for \lambda^P. in(z, t)
                divide in(z, t) \vee notin(z, t)
                  case in(z, t) {
                    apply invoke adj_cor1 for t, x, z
                    connect in(z, A) \wedge in(z, adj(t, x))
                    apply invoke leftward cap_characterization for A, adj(t, x), z
                    connect in(z, cap(A, adj(t, x))) \wedge neq(z, y)
                    apply invoke minimal_core(in, cap(A, adj(t, x)), y) for z
                  }
                  case notin(z, t) {
                    deny in(z, y) {
                      connect in(z, y) \wedge in(y, adj(t, x))
                      apply invoke trans(adj(t, x)) for y, z
                      apply invoke rightward adj_existence for t, x, z
                      divide in(z, t) \vee equal(z, x)
                        case in(z, t) {
                          contradict in(z, t)
                          explode equal(z, x)
                        }
                        case equal(z, x) {}
                      substitute in(z, y) for z: x
                      deny equal(x, y) {
                        substitute in(x, y) for y: x
                        apply invoke regular_is_irrefrel_wellfounded for x
                        split 1 irrefrel(x, in) \wedge wellfounded(x, in)
                        apply invoke irrefrel_not_contain_itself for x
                        contradict in(x, x)
                      }
                      apply invoke rightward adj_existence for t, x, y
                      divide in(y, t) \vee equal(y, x)
                        case in(y, t) {}
                        case equal(y, x) {
                          contradict equal(y, x)
                          explode in(y, t)
                        }
                      connect in(x, y) \wedge in(y, t)
                      apply invoke trans(t) for y, x
                      connect in(x, cap(A, adj(t, x))) \wedge neq(x, y)
                      apply invoke minimal_core(in, cap(A, adj(t, x)), y) for x
                      contradict in(x, y)
                    }
                  }
              }
            }
            fold for minimal_core conclude minimal_core(in, A, y)
            connect minimal(in, A, y)
            lift for y conclude \exists y (minimal(in, A, y))
          }
        }
      }
    }
  }
  fold for pwellfounded conclude pwellfounded(regular, in)
}

theorem regular_is_closed_under_in pclosed(regular, in) {
  any x, y {
    assume regular(x) \wedge in(y, x) {
      apply invoke element_of_regular_is_regular for x, y
    }
  }
  fold for pclosed conclude pclosed(regular, in)
}

theorem regular_is_tcsetlike tcsetlike(regular, in) {
  any x {
    assume regular(x) {
      apply invoke rightward regular_iff_existence_of_irrefrel_wellfounded_tcm for x
      some t such \exists t (irrefrel(t, in) \wedge wellfounded(t, in) \wedge tcm(x, t)) {
        split 3 irrefrel(t, in) \wedge wellfounded(t, in) \wedge tcm(x, t)
        expand tcm(x, t) for tcm
        lift for t conclude \exists t (tcset(x, in, t))
      }
    }
  }
  fold for tcsetlike conclude tcsetlike(regular, in)
}

theorem regular_induction \forall^P \chi[1] (\forall x (regular(x) \wedge \forall y (in(y, x) \to \chi(y)) \to \chi(x)) \to \forall x (regular(x) \to \chi(x))) {
  assert regular_is_pirrefrel
  assert regular_is_pwellfounded
  assert regular_is_closed_under_in
  assert regular_is_tcsetlike
  apply invoke tcsetlike_implies_rtcsetlike for regular, in
  any predicate \chi[1] {
    assume \forall x (regular(x) \wedge \forall y (in(y, x) \to \chi(y)) \to \chi(x)) {
      connect rtcsetlike(regular, in) \wedge pclosed(regular, in) \wedge pirrefrel(regular, in) \wedge pwellfounded(regular, in) \wedge \forall x (regular(x) \wedge \forall y (in(y, x) \to \chi(y)) \to \chi(x))
      apply invoke pwellfounded_induction for regular, in, \chi
    }
  }
}
